void run_n_simul(double *T, double J, int n, int size, int mc,
                 int trans, char* file);

void run_multiple_sizes(double *T, int t_len, int *sizes, int sizes_len,
                        double J, int mc, int trans, char *file);

void read_n_run();
